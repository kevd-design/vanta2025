/**
 * Editor Configuration
 * 
 * These settings can only be changed by developers with access to the code.
 * Changes require a rebuild/redeploy of the Sanity Studio.
 */
export const EDITOR_CONFIG = {
  /**
   * When true, all image fields in siteSettings and companySettings 
   * will be set to read-only mode.
   */
  READ_ONLY_IMAGES: true,
  
}